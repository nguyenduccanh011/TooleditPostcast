<Window
    x:Class="PodcastVideoEditor.Ui.Views.NewProjectDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="New Project"
    Width="500"
    Height="280"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource PrimaryBackgroundBrush}"
    Foreground="{StaticResource PrimaryForegroundBrush}"
    ShowInTaskbar="False"
    WindowStyle="SingleBorderWindow">

    <Grid>
        <StackPanel Margin="20" VerticalAlignment="Stretch">
            <!-- Title -->
            <TextBlock
                Text="Create New Project"
                FontSize="18"
                FontWeight="Bold"
                Margin="0,0,0,20"
                Foreground="{StaticResource AccentBlueBrush}" />

            <!-- Project Name -->
            <TextBlock Text="Project Name:" Margin="0,0,0,5" />
            <TextBox
                x:Name="ProjectNameTextBox"
                Height="35"
                Padding="10"
                Background="{StaticResource SecondaryBackgroundBrush}"
                Foreground="{StaticResource PrimaryForegroundBrush}"
                BorderBrush="{StaticResource AppCardBorderBrush}"
                BorderThickness="1"
                Margin="0,0,0,15"
                ToolTip="Enter a name for your project" />

            <!-- ...existing code... -->

            <!-- Buttons -->
            <Grid Height="40" VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="CreateButton"
                    Grid.Column="1"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Content="Create"
                    Style="{StaticResource SuccessActionButtonStyle}"
                    Click="CreateButton_Click"
                    Cursor="Hand" />

                <Button
                    x:Name="CancelButton"
                    Grid.Column="2"
                    Width="100"
                    Height="35"
                    Content="Cancel"
                    Background="{StaticResource NeutralButtonBrush}"
                    Foreground="{StaticResource PrimaryForegroundBrush}"
                    BorderThickness="0"
                    Click="CancelButton_Click"
                    Cursor="Hand" />
            </Grid>
        </StackPanel>
    </Grid>
</Window>
