<Window
    x:Class="PodcastVideoEditor.Ui.Views.AIAnalysisProgressWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Phân tích AI"
    Width="480"
    Height="320"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource PrimaryBackgroundBrush}"
    Foreground="{StaticResource PanelForegroundBrush}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStyle="SingleBorderWindow">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="✦ Đang phân tích AI…"
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="{StaticResource AccentPurpleBrush}" Margin="0,0,0,16"/>

        <!-- Step label -->
        <TextBlock Grid.Row="1"
                   Text="{Binding StepMessage}"
                   FontSize="13" Foreground="{StaticResource PanelForegroundBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,10"/>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="2"
                     Value="{Binding ProgressPercent}"
                     Minimum="0" Maximum="100"
                     Height="12" VerticalAlignment="Top"
                     Foreground="{StaticResource AccentPurpleBrush}" Background="{StaticResource SeparatorBrush}"
                     BorderThickness="0" Margin="0,0,0,12"/>

        <!-- Error detail (shows only on error) -->
        <ScrollViewer Grid.Row="3"
                      MaxHeight="80"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                      Margin="0,0,0,12">
            <TextBlock Text="{Binding ErrorDetail}"
                       Foreground="{StaticResource AccentRedBrush}" FontSize="11"
                       TextWrapping="Wrap"/>
        </ScrollViewer>

        <!-- Cancel button -->
        <Button Grid.Row="4"
                Content="Huỷ"
                Command="{Binding CancelCommand}"
                HorizontalAlignment="Right"
                Background="{StaticResource SeparatorBrush}" Foreground="{StaticResource PanelForegroundBrush}"
                Padding="20,8" MinHeight="32"/>
    </Grid>
</Window>
