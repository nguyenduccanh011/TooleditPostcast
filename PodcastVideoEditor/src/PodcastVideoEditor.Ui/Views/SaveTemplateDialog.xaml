<Window
    x:Class="PodcastVideoEditor.Ui.Views.SaveTemplateDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Save Template"
    Width="520"
    Height="320"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource PrimaryBackgroundBrush}"
    Foreground="{StaticResource PrimaryForegroundBrush}"
    ShowInTaskbar="False"
    WindowStyle="SingleBorderWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Save Current Layout As Template"
            FontSize="18"
            FontWeight="Bold"
            Margin="0,0,0,16"
            Foreground="{StaticResource AccentLightBlueBrush}" />

        <TextBlock Grid.Row="1" Text="Template name" Margin="0,0,0,6" />
        <TextBox
            Grid.Row="2"
            x:Name="TemplateNameTextBox"
            Height="34"
            Padding="10,6"
            Background="{StaticResource SecondaryBackgroundBrush}"
            Foreground="{StaticResource PrimaryForegroundBrush}"
            BorderBrush="{StaticResource AppCardBorderBrush}"
            BorderThickness="1"
            Margin="0,0,0,12" />

        <StackPanel Grid.Row="3">
            <TextBlock Text="Description" Margin="0,0,0,6" />
            <TextBox
                x:Name="DescriptionTextBox"
                Height="92"
                Padding="10"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto"
                Background="{StaticResource SecondaryBackgroundBrush}"
                Foreground="{StaticResource PrimaryForegroundBrush}"
                BorderBrush="{StaticResource AppCardBorderBrush}"
                BorderThickness="1" />
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="ValidationText" VerticalAlignment="Center" Foreground="{StaticResource WarningForegroundBrush}" />

            <Button
                Grid.Column="1"
                Width="96"
                Height="34"
                Margin="0,0,8,0"
                Content="Cancel"
                Background="{StaticResource NeutralButtonBrush}"
                BorderThickness="0"
                Click="CancelButton_Click" />

            <Button
                Grid.Column="2"
                Width="120"
                Height="34"
                Content="Save Template"
                Background="{StaticResource AccentGreenBrush}"
                BorderThickness="0"
                Click="SaveButton_Click" />
        </Grid>
    </Grid>
</Window>
